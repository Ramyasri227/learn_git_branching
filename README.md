# Learn Git Branching

This Contain the Conpects of Learn Git Branching in Git learn i have learned about the basic commands of the Git like git commit, git merge, git branch, git checkout, git rebase and many more..

**Learn Git Branching** will also explain the what is git commit, git merge, git branch, git checkout, git rebase based on the levels we will go on.

* In this Learn Git branching I have completed few Sections and I have made a markdown file for each Section.
* In each section they are few levels and for each level I have posted what are the commands we need to follow for each and every step.

**Section1**

In Section 1, we will discuss Git commit, branch, merge, and rebase with the help of this section, we will understand how these commands are used in Git.

**Section2**

In these Section2 we will discuss about the Git HEAD, Detaching Head, Relative References (^) & (~) and Revert with the help of this section we will understand how these commands are used in Git.

**Section3**


